version: '3.8'

services:
  # LocalStack - AWS services mock (DynamoDB, Lambda, etc.)
  localstack:
    container_name: esiritori-localstack
    image: localstack/localstack:latest
    ports:
      - "4566:4566"
      - "4510-4559:4510-4559"
    environment:
      - SERVICES=dynamodb,lambda,iam,logs
      - DEBUG=1
      - DATA_DIR=/tmp/localstack/data
      - LAMBDA_EXECUTOR=docker
      - DOCKER_HOST=unix:///var/run/docker.sock
      - HOST_TMP_FOLDER=${TMPDIR:-/tmp/}localstack
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "./tmp/localstack:/tmp/localstack"
    networks:
      - esiritori-network

  # Next.js フロントエンド開発サーバー
  frontend:
    container_name: esiritori-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
      - NEXT_PUBLIC_WS_URL=ws://localhost:3001
      - DYNAMODB_ENDPOINT=http://localstack:4566
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - localstack
    networks:
      - esiritori-network
    command: pnpm dev

  # API モックサーバー（将来的にSpring Bootに置き換え）
  api-mock:
    container_name: esiritori-api-mock
    build:
      context: ./frontend/mock
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - DYNAMODB_ENDPOINT=http://localstack:4566
      - AWS_REGION=ap-northeast-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
    depends_on:
      - localstack
    networks:
      - esiritori-network

  # DynamoDB テーブル初期化 + Lambda デプロイ
  aws-init:
    container_name: esiritori-aws-init
    image: amazon/aws-cli:latest
    volumes:
      - ./infrastructure/schemas:/schemas
      - ./scripts:/scripts
      - ./backend:/backend
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - AWS_REGION=ap-northeast-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
    depends_on:
      - localstack
    networks:
      - esiritori-network
    entrypoint: >
      /bin/sh -c "
        echo 'Installing dependencies...';
        yum update -y && yum install -y jq zip;
        echo 'Waiting for LocalStack to be ready...';
        until aws dynamodb list-tables --endpoint-url http://localstack:4566 --region ap-northeast-1 > /dev/null 2>&1; do
          echo 'Waiting for LocalStack...';
          sleep 2;
        done;
        echo 'LocalStack is ready, creating DynamoDB tables...';
        /schemas/create-tables.sh http://localstack:4566 ap-northeast-1 /schemas/dynamodb-table-definitions.json;
        echo 'DynamoDB tables created successfully!';
        echo 'Setup completed!';
        sleep infinity;
      "

  # DynamoDB 管理UI - LocalStack DynamoDBの表示・管理
  # http://127.0.0.1:8001/
  dynamodb-admin:
    container_name: esiritori-dynamodb-admin
    image: aaronshaf/dynamodb-admin:latest
    ports:
      - "8001:8001"
    environment:
      - DYNAMO_ENDPOINT=http://localstack:4566
      - AWS_REGION=ap-northeast-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
    depends_on:
      - aws-init
    networks:
      - esiritori-network

networks:
  esiritori-network:
    driver: bridge

volumes:
  localstack-data:
